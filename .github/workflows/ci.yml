name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Tests Python à réimplémenter pour la structure src/ kubewi
# bootstrap/ supprimé — ce workflow est désactivé jusqu'à réécriture
jobs:
  placeholder:
    name: CI (placeholder)
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI à implémenter pour src/kubewi"
