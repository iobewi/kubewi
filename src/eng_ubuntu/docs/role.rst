Rôle
====

``eng_ubuntu`` est le pendant de ``eng_debian`` pour les nœuds Ubuntu.
Il est actuellement un placeholder — le playbook existe mais le rôle Ubuntu
n'est pas encore implémenté.

Prévu pour les workers nécessitant des drivers Ubuntu spécifiques
(ex : nœuds Intel avec stack DPDK ou OpenVINO).
